# 2026-03-14 (Day 16)

## Goal

Transition the robot from a standard circular model to a custom **rectangular chassis** and optimize the Nav2 stack parameters for the new footprint, while performing a full workspace cleanup to ensure project hygiene.

## What I did

- **Robot Modeling (URDF)**:
    - Redesigned the chassis in `my_robot.urdf` from a default cylinder to a **$0.6m \times 0.4m$ rectangle**.
    - Updated the `visual` and `collision` properties to match the new physical dimensions for accurate physics engine interaction.
- **Nav2 Parameter Tuning**:
    - Replaced the default `robot_radius: 0.22` with a precise **footprint coordinate array** in `nav2_params.yaml`.
    - Coordinates set to: `[[0.3, 0.2], [0.3, -0.2], [-0.3, -0.2], [-0.3, 0.2]]`.
    - Applied these changes to both `local_costmap` and `global_costmap` to ensure the robot accounts for its width during obstacle avoidance.
- **Workspace & Git Hygiene**:
    - Identified a major Git tracking issue involving **4,418 untracked build/log files**.
    - Updated `.gitignore` to exclude `build/`, `install/`, and `log/` directories.
    - Executed `git rm --cached` to reset the repository index and maintain a source-only state.
- **System Integration & Troubleshooting**:
    - Launched the simulation using the updated launch scripts.
    - Diagnosed a path-resolution issue where the `turtlebot3_world.world` failed to load via relative paths.

## Result

- The robot successfully spawns in Gazebo as a **custom rectangular model**.
- The Navigation2 stack successfully integrated the new `footprint` parameters, enabling shape-aware path planning.
- The workspace is now **clean and professional**, with zero build artifacts or unnecessary logs in the version control system.
- Confirmed that the Nav2 planner now treats the robot as a $0.6m \times 0.4m$ object rather than a single point.

## Conclusion

The core transition to a custom rectangular robot platform is successfully implemented. By moving from a circular radius to a polygonal footprint, the navigation system is now significantly more accurate for real-world chassis shapes. 

While the automated world-loading path requires refinement, the software configuration and repository structure are now optimized and fully compliant with project standards.

The navigation pipeline is verified as:
**map_server → AMCL → planner_server (Footprint-aware) → controller_server (Rectangular avoidance) → robot motion**