## Day18 - Custom Gazebo World Construction

### What I completed
- Reset the simulation environment and terminated all running Gazebo processes
- Cleaned the previous `world` directory and removed outdated models
- Recreated the world directory structure:
  `ros2-nav-learning/world/my_world/my_world.world`
- Successfully launched Gazebo using the custom world file
- Began constructing a custom indoor simulation environment in Gazebo
- Built a basic room structure using primitive models:
  - `Box` models for walls
  - `Cylinder` models for obstacles
  - `Sphere` models for obstacles
- Created a basic enclosed environment with an entrance opening for robot access

### What worked
- Gazebo successfully loads the custom `.world` file
- Primitive models can be inserted and positioned inside the environment
- The environment layout is suitable for future SLAM experiments
- The project now supports running navigation experiments inside a custom world

### Current limitations
- Dynamic physics objects caused noticeable jitter during environment construction
- Some objects intersected with the ground plane during placement
- Environment objects are currently still dynamic instead of static
- The simulation world still needs refinement for stable physics behavior

### Current project status
The custom robot project has now progressed from:

- navigation testing inside default Gazebo environments

to:

- running experiments inside a custom built Gazebo simulation world

This is an important step toward creating a fully controlled navigation testing environment.

More work is still required before the environment becomes stable enough for full SLAM and navigation validation.

### Next steps
- Convert environment objects to static models
- Stabilize the Gazebo world physics behavior
- Finalize the custom world layout
- Run SLAM Toolbox inside the custom environment
- Generate and save the occupancy grid map
- Prepare the map for Nav2 navigation testing